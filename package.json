{
  "name": "flat4b-expenses-root",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "dotenv": "^16.6.1"
  },
  "scripts": {
    "postinstall": "echo '=== INSTALLING BACKEND ===' && cd backend && npm install && echo '=== INSTALLING FRONTEND ===' && cd ../frontend && npm install && npm run build && echo '=== BUILD SUCCESSFUL ==='",
    "build": "npm run postinstall",
    "start": "cd backend && npm start"
  }
}
